inttiize serial communication of 9600 bits per seconds

Sertal.begin(9600);

// the loop routine nuns over and over anoin forevers

void loop

// read the topution analog ti

int sensorValue = analogRead(A0);

// print out the value you redd:

Serial.println(sensorValue);

delay(1);

delicy in between reads for stoo1lity
